import { StyleSheet } from "react-native";

export const formStyles = StyleSheet.create({
  errorText: {
    color: "#dc2626",
    fontSize: 12,
    marginTop: 2,
  },
  inputError: {
    borderColor: "#dc2626",
  }
});